{
    "resource_cd": "ceh.{{ ctx.schema }}.{{ ctx.name }}",
    "resource_desc": "Таблица {{ ctx.schema }}.{{ ctx.name }}",
    "tags": [
        "zi21",
        "{{ ctx.src_cd }}"
    ],
    "features": {},
    "datasets": [
        {
            "name": "{{ ctx.name }}",
            "schema_name": "{{ ctx.schema }}"
        }
    ],
    "is_readonly": false
}